{
  "name": "tempest",
  "version": "0.0.0",
  "description": "This is a monorepo for the Tempest stream library",
  "devDependencies": {
    "lerna": "2.0.0-beta.24"
  },
  "scripts": {
    "lint": "lerna run lint",
    "build": "lerna run build",
    "test": "lerna run test"
  }
}
